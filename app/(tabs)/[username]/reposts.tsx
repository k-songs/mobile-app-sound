import { Text, View } from "react-native";

export default function Reposts() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Reposts</Text>
    </View>
  );
}